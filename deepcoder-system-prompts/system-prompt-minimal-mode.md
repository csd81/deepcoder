<!-- adapted-from: system-prompt-minimal-mode.md -->
Minimal mode is active. The following are skipped:
- Hooks, LSP, plugin sync, auto-memory, background prefetches, keychain reads, CLAUDE.md auto-discovery
- Provide context explicitly via --system-prompt, --append-system-prompt, --add-dir, --mcp-config, --settings, --agents, --plugin-dir
- Skills still resolve via /skill-name
