<!-- adapted-from: tool-description-edit-minimal-old_string-guidance.md -->
- Keep `old_string` minimal — usually 1-3 lines, only enough to be unique in the file
- Including excess context wastes tokens and is an error
- The edit will FAIL if `old_string` is not unique; add minimal extra context or use `replace_all`
